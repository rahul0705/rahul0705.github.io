---
name: 'Setup Project'
description: 'Checkouts code, setups Node.js, and installs dependencies'

runs:
  using: 'composite'
  steps:
    - uses: actions/setup-node@v6 # zizmor: ignore[unpinned-uses]
      with:
        node-version: lts/*
        cache: npm

    - run: npm ci
      shell: bash
