export interface ResumeInterest {
  name: string;
  keywords?: string[];
  href?: string;
}

// Add interests as named groups, with optional examples, related topics, and a web-only link.
export const interests: ResumeInterest[] = [];
